import { makeStyles } from "@mui/styles";


const useStyles = makeStyles((theme) => ({
    contSpinner:{
        paddingLeft: "40%",
        paddingBottom: "20px",
        paddingTop: "20px",
    }
}));


export default useStyles;